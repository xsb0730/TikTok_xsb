<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 外部存储 -->
    <external-path
        name="external"
        path="." />

    <!-- 外部缓存 -->
    <external-cache-path
        name="external_cache"
        path="." />

    <!-- 内部缓存 -->
    <cache-path
        name="cache"
        path="." />

    <!-- 内部文件 -->
    <files-path
        name="files"
        path="." />
</paths>